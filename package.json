{
  "name": "bins",
  "private": true,
  "type": "module",
  "description": "Front-end sources for the Bin day app. No dependencies; node is used only to run the resolver tests.",
  "scripts": {
    "test": "node --test test/*.test.mjs"
  }
}
